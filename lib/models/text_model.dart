import 'package:flutter/material.dart';

class TextModel {
  String text;
  double fontSize;
  Offset offset;
  Color textColor;

  TextModel({this.fontSize, this.offset, this.text, this.textColor});
}
